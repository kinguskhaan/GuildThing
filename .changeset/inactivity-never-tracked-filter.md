---
"@guildthing/web": patch
---

Adds an "Only never tracked" filter to the bulk inactivity manager, so you can isolate the hundreds of members who've never sent a tracked message and select them all at once for "Reset activity to now" — previously there was no way to narrow the list down to just the untracked ones.
