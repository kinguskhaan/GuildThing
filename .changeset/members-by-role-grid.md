---
"@guildthing/web": minor
---

Reworks the "Members by role" panel into a grid: filtering to a role now shows every other role each matched member holds too, as toggleable columns — not just the filter role. Click a column header to check/uncheck that role for every visible row at once (e.g. filter to "Core Raider", clear "PUG" off all of them in one go). Still fully staged/batched — nothing hits Discord until you apply.
