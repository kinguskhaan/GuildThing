---
"@guildthing/web": patch
---

The "Members by role" grid's Name column now shows each member's actual server nickname (falling back to their account tag if they have none set), with the tag shown alongside in parentheses when a nickname is set — it was showing the account username/tag only before, which doesn't match what's actually visible in the Discord member list.
