---
"@guildthing/web": minor
---

- The Members page's wide tables (roster, nicknames, external characters, members-by-role) now use the full page width and up to 70% of the viewport height instead of being squeezed into a narrow, short box — the members-by-role tab specifically wasn't getting the wider layout other tabs already had.
- Added a "Download as Markdown" button to the members-by-role grid — saves exactly what's on screen (including any unapplied staged checkbox changes) as a `.md` table file.
